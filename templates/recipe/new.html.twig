{% extends 'base.html.twig' %}

{% block title %}Nouvelle recette - Gourmand{% endblock %}

{% block body %}
<div class="container py-4" style="max-width: 800px;">

    {# En-tête avec titre et bouton de retour #}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h2 mb-0">➕ Créer une recette</h1>
        <a href="{{ path('app_recipe_index') }}" class="btn btn-outline-secondary">
            ⬅️ Retour à la liste
        </a>
    </div>

    {# Carte du formulaire #}
    <div class="card shadow-sm border-0 p-4">
        {{ include('recipe/_form.html.twig', {'button_label': '💾 Enregistrer la recette'}) }}
    </div>

</div>
{% endblock %}